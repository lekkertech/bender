# Copy to .env and fill in values. Do not commit real secrets.

# Required
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Socket Mode (default)
SLACK_APP_TOKEN=xapp-your-app-token

# Events API (HTTP) — if using HTTPS instead of Socket Mode
# SLACK_SIGNING_SECRET=your-signing-secret
# PORT=3000

# Optional
LOG_LEVEL=info
TIMEZONE=Africa/Johannesburg
ALLOWED_CHANNELS=C0919MX7KJS
DEFAULT_REPLY_MODE=channel

# Feature toggles (comma-separated). Default is "boom,fun" if unset.
# Valid values: boom,fun
# FEATURES=boom,fun

# Fun feature (OpenAI)
# Set your OpenAI key to enable fun commands (@bot haiku, roast, etc).
# If missing, the bot will reply that AI is not configured.
# Model default is gpt-4.1-nano unless overridden.
# OPENAI_API_KEY=sk-your-openai-key
# OPENAI_MODEL=gpt-4.1-nano

# Optional override allowlist for Fun feature only (comma-separated channel IDs).
# If unset, Fun responds in any channel the bot is in.
# FUN_ALLOWED_CHANNELS=C0123,C0456


# Optional JSON config for Fun commands (see data/fun-commands.json.example)
# FUN_CONFIG=data/fun-commands.json
